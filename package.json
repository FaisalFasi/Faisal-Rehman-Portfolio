{
  "name": "faisal-rehman-portfolio",
  "version": "1.0.0",
  "description": "A Bootstrap boilerplate",
  "main": "index.js",
  "scripts": {
    "watch": "sass --watch scss/style.scss css/style.css",
    "clean": "rm -rf ./css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "'your name'",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.0-alpha1"
  },
  "devDependencies": {
    "sass": "^1.58.0"
  }
}
